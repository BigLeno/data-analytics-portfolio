services:
  app:
    build: .
    container_name: aprovaedu-analytics
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./reports:/app/reports
    environment:
      - PYTHONUNBUFFERED=1